# Streamlit Community Cloud does not read PEP 735 [dependency-groups] from
# pyproject.toml, so the demo's runtime dependencies are mirrored here.
# Keep in sync with the cpu-sanity and demo groups in pyproject.toml.
#
# CPU-only wheels: the default PyPI torch build pulls bundled CUDA libraries
# (multiple GB) that a CPU-only host cannot use and does not have room for.
--extra-index-url https://download.pytorch.org/whl/cpu

coqui-tts==0.27.5
coqui-tts-trainer==0.3.3
numpy>=1.26,<2
soundfile>=0.13
torch==2.8.0
torchaudio==2.8.0
torchvision>=0.22,<0.24
transformers>=4.57,<5
streamlit>=1.61.1
