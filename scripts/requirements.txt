# Streamlit Community Cloud checks the app directory before the repository
# root. Keep this file beside demo_streamlit_adamawa.py so it takes precedence
# over the root uv.lock, which intentionally contains only the lightweight
# fub-tts package in its default environment.
#
# Keep these runtime dependencies in sync with the cpu-sanity and demo groups
# in ../pyproject.toml.
#
# CPU-only wheels: the default PyPI torch build pulls bundled CUDA libraries
# (multiple GB) that a CPU-only host cannot use and does not have room for.
--extra-index-url https://download.pytorch.org/whl/cpu

coqui-tts==0.27.5
coqui-tts-trainer==0.3.3
numpy>=1.26,<2
soundfile>=0.13
torch==2.8.0
torchaudio==2.8.0
torchvision>=0.22,<0.24
transformers>=4.57,<5
streamlit>=1.61.1
